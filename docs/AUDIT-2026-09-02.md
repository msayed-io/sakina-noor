# دراسة مكثفة لمشروع سَكِينَة — 2026-09-02

> وثيقة مرجعية داخلية. كل رقم وكل نتيجة فيها مُتحقَّق منها بأمر فعلي على النسخة المحلية، مش تقدير.

---

## 1) حالة السحب والتحقق من السلامة

| البند | النتيجة |
|---|---|
| المستودع | `msayed-io/sakina-noor` |
| الفرع | `main` (الوحيد، لا توجد فروع أو tags أخرى) |
| HEAD محليًا | `692c67c256a1f43aecfed8cec18de087a7ad48b3` |
| HEAD على GitHub (`git ls-remote`) | `692c67c256a1f43aecfed8cec18de087a7ad48b3` — **مطابق** |
| عدد الملفات في الشجرة | 113 ملفًا (مطابق لعدد الملفات على القرص) |
| `git fsck --full` | نظيف |
| `git status --porcelain` | 0 سطر (لا ملفات ناقصة ولا معدّلة) |
| آخر commit | `Refine azkar section with Sakina Pro cards` — 2026-09-02 10:32 UTC |
| إجمالي commits | 13 |
| الحجم | 4.1 MB على القرص |

**الخلاصة:** النسخة كاملة 100%، ولا يوجد ملف ناقص.

---

## 2) البنية الكاملة (113 ملفًا)

```
sakina-noor/
├── .github/workflows/ci.yml        CI: bun install → lint → build
├── .gitignore
├── Caddyfile                       reverse proxy :81 → :3000 (أداة المنصة الأصلية)
├── README.md                       69 سطر، عربي
├── bun.lock                        lockfile (bun)
├── components.json                 shadcn config (style: new-york)
├── eslint.config.mjs               أغلب القواعد معطّلة
├── next.config.ts                  output: standalone + security headers
├── package.json                    Next 16 / React 19 / Tailwind 4
├── postcss.config.mjs              @tailwindcss/postcss فقط
├── tailwind.config.ts              ⚠️ غير محمّل (انظر §6.6)
├── tsconfig.json                   strict، noImplicitAny: false
├── vercel.json                     framework: nextjs، bun install/build
├── prisma/schema.prisma            User + Post (غير مستخدم في صفحة الهبوط)
├── docs/ (9 ملفات)                 مراجعات تصميم سابقة + خريطة الخطوط + ملاحظات QCF
├── download/README.md              سطر واحد (بقايا أداة)
├── examples/websocket/             مثال غير مُدرج في tsconfig (مستثنى)
├── mini-services/.gitkeep          فارغ
├── tests/*.sh (3)                  ⚠️ تعتمد على `.zscripts/` غير الموجود
├── public/
│   ├── logo.svg (1 KB)             أيقونةfavicon
│   ├── og-image.svg (1.5 KB)       ⚠️ SVG كـ OG image (انظر §6.8)
│   ├── fonts/qcf/p001.woff2 (41 KB)
│   ├── fonts/sura_names.woff2 (88 KB)
│   ├── fonts/thmanyahserifdisplay-Black.woff2 (79 KB) ⚠️ مكرر
│   ├── fonts/sakina/ (8 ملفات)     ⚠️ منها ملفان غير مستخدمين (2.0 MB)
│   └── images/quran/surah-header-frame.webp (460 KB) ⚠️ lossless
└── src/
    ├── app/
    │   ├── layout.tsx      67 سطر — metadata + JSON-LD + <html lang=ar dir=rtl>
    │   ├── page.tsx        34 سطر — ترتيب الأقسام العشرة
    │   ├── globals.css     2642 سطر — ⭐ نظام التصميم كله هنا
    │   ├── robots.ts / sitemap.ts
    │   └── api/route.ts    "Hello, world!" (boilerplate)
    ├── components/
    │   ├── landing/ (11 مكوّن) ⭐ المشروع الفعلي
    │   └── ui/ (48 ملف shadcn) ⚠️ غير مستخدم إطلاقًا
    ├── hooks/ (2)          use-mobile، use-toast — غير مستخدمة
    └── lib/ (2)            utils.ts (مستخدمة)، db.ts (غير مستخدمة)
```

---

## 3) ترتيب الصفحة (10 أقسام)

`page.tsx` يرتّبها كالتالي، وهذا هو العقد البصري للمشروع:

| # | المكوّن | `id` | الأسطر | نوعه |
|---|---|---|---|---|
| 1 | `Header` | `nav` | 52 | client (scroll state) |
| 2 | `Hero` | — | 157 | client (parallax) |
| 3 | `Manifesto` | — | 49 | server |
| 4 | `Features` | `#features` | 130 | server |
| 5 | `QuranPreview` | `#quran` | 215 | client (مشغّل صوت) |
| 6 | `AzkarSection` | `#azkar` | 110 | client (بلا JS فعلي) |
| 7 | `PrayerSection` | `#prayer` | 63 | server |
| 8 | `StatsSection` | — | 33 | server |
| 9 | `FinalCta` | `#download` | 35 | server |
| 10 | `Footer` | — | 37 | server |
| — | `ScrollEffects` | — | 55 | client (IntersectionObserver + smooth anchor) |

---

## 4) نظام التصميم (Design Tokens)

المرجع الوحيد: `:root` في `globals.css:10`، ومتكرر حرفيًا في `globals.css:2474` (طبقة Sakina Pro).

### الألوان

| Token | القيمة | الاستخدام |
|---|---|---|
| `--cream` | `#ece7de` | خلفية الصفحة |
| `--cream-light` | `#f4eee2` | تدرجات |
| `--cream-dark` | `#dfd7c8` | تدرجات |
| `--ink` | `#2b1a10` | النص الأساسي (بني داكن) |
| `--ink-soft` | `#3a2517` | تدرج داكن |
| `--ink-deep` | `#180d07` | شاشة الموبايل |
| `--ink-muted` | `#7f6a55` | النص الثانوي |
| `--ink-faded` | `#a89680` | — |
| `--honey` | `#b88a4f` | ⭐ اللون المميّز (accent) |
| `--honey-light` | `#deab65` | الذهبي الفاتح |
| `--honey-deep` | `#9a6f3a` | الذهبي الداكن |
| `--gold` / `-light` / `-deep` | `#c9a86a` / `#deab65` / `#9a6f3a` | مرادفات |
| `--paper` | `#fff9f1` | نص على الداكن |
| `--sage` | `#80a390` | ✅ علامة الصح (أخضر) |
| `--sage-deep` | `#5a7d6a` | — |
| `--dusk` / `--dusk-deep` | `#8ca1b3` / `#667a8c` | أيقونة المساء |

### الخطوط (محلية 100%، لا Google Fonts)

| المتغير | العائلة | الملفات | الأوزان |
|---|---|---|---|
| `--ff` | `Thmanyah Sans` | `thmanyahsans-Regular/Bold` | 400، 700–900 |
| `--fq` | `Thmanyah Serif Text` | `thmanyahseriftext-Regular/Medium` | 400، 500–700 |
| `--fd` | `Thmanyah Serif Display` | `thmanyahserifdisplay-Bold/Black` | 700، 900 |
| — | `QCF_P001` | `qcf/p001.woff2` | صفحة المصحف (glyphs خاصة) |
| — | `SuraNames` | `sura_names.woff2` | عنوان السورة (غير مستخدم حاليًا) |

**القاعدة الحاكمة:** `globals.css:2596-2600` تفرض الخطوط بـ`!important` على `html,body` وكل العناوين والنصوص. أي تعديل خط لازم يمر من هنا.

### primitives الزجاجية (Sakina Pro)

معرّفة في `globals.css:2508-2524`:
- `.cut-crystal-panel` (2508) — blur 26px / saturate 210%
- `.cut-crystal-capsule` (2518) — blur 30px، radius 9999px
- `.cut-crystal-capsule-dark` (2520) — داكن + حدود ذهبية
- `.cut-crystal-capsule-gold` (2521) — `#deab65`
- `.cut-crystal-satin` (2522) — blur 20px، radius 28px
- `.gem-rim-glow` (2524) — لمعة حواف

> ملاحظة: `.cut-crystal-panel` معرّف **مرتين** — مرة في `:110` (بمتغيرات محلية) ومرة في `:2508` (قيم Sakina Pro المثبّتة). الثانية هي الفعّالة.

### مقاييس

| Token | القيمة |
|---|---|
| `--cm` (أقصى عرض) | `1200px` |
| `--cp` (حشو جانبي) | `1.5rem` |
| `--sp` (حشو رأسي للأقسام) | `7rem` |
| `--rc` / `--rg` / `--rg2` / `--rp` | `1.5rem` / `1.75rem` / `2.25rem` / `9999px` |
| `--card-radius` | `28px` |
| Easing | `--ef` cubic-bezier(.32,.72,0,1)، `--es` (.34,1.56,.64,1) |
| Durations | `--df` 200ms، `--db` 400ms، `--ds` 800ms، `--dc` 1200ms |
| Breakpoints | 980 / 900 / 640 / 560 / 480 px |

---

## 5) خريطة `globals.css` (2642 سطرًا)

الملف مبني بطبقات متراكمة، واللاحق يتجاوز السابق:

| السطر | الطبقة |
|---|---|
| 3 | الأساس + `:root` tokens |
| 106 | Navigation |
| 251 | Hero |
| 458 | إطار iPhone 16 Pro (CSS خالص: تيتانيوم + Dynamic Island + أزرار جانبية) |
| 913 | Manifesto |
| 1040 | Features Bento |
| 1216 | Quran Showcase |
| 1338 | ⚠️ Mushaf قديم — **CSS ميت** (~244 سطر) |
| 1582 | Azkar |
| 1707 | Prayer |
| 1928 | Stats |
| 2035 | Final CTA + Footer |
| 2228 | Scroll Reveal |
| 2257 | QCF v2 preview (وفيها classes ميتة) |
| 2340 | صفحة المصحف الدقيقة (17em × 27.75em) |
| 2470 | ⭐ طبقة Sakina Pro — tokens + mapping |
| 2544 | ⭐ الخطوط الرسمية |
| 2605 | ⭐ Prayer dashboard النهائي |
| 2631 | ⭐ Azkar النهائي (12 عمود) |

**قاعدة ذهبية لأي تعديل:** الأقسام 2605 و2631 و2470 هي **الكلمة الأخيرة** في CSS. أي تغيير في الأقسام الأولى (1582/1707) قد لا يظهر لأن الطبقات الأخيرة تتجاوزه.

---

## 6) سجل الملاحظات (مرتّب بالأهمية)

### 🔴 أ — يؤثر على المستخدم/الإنتاج

**أ1. `Thmanyah Serif Display` معرّف مرتين بشكل متعارض**
- `globals.css:2344` → ملف `Black` بوزن افتراضي (400) و`font-display: swap`
- `globals.css:2583` → نفس الملف بوزن 900
- **الأثر:** أي عنصر يطلب وزن 400 سيُطابَق بوجه Black. مثال حي: `.prayer-current-copy strong` و`.prayer-current-card .prayer-ring-time` (`globals.css:2625`) يحدّدان `font-family:'Thmanyah Serif Display'` لكن **بدون `font-weight`**، فيرثان 400 من body → يُعرضان بخط Black الثقيل بدل Bold.
- **الحل:** حذف البلوك الأول (2344–2348) وإعادة تعريف `Thmanyah Serif Display` بوزن 400 من ملف Bold إذا لزم.

**أ2. ملف خط مكرر بنفس البايتات**
```
651bfb483caaf24ca660900c29256014  public/fonts/thmanyahserifdisplay-Black.woff2
651bfb483caaf24ca660900c29256014  public/fonts/sakina/thmanyahserifdisplay-Black.woff2
```
نفس الملف (79,212 بايت) منشور على مسارين، وكلاهما يُرجع 200.

**أ3. 2.0 MB خطوط منشورة ولا يشير إليها أي `@font-face`**
| الملف | الحجم | مراجع في CSS |
|---|---|---|
| `public/fonts/sakina/QCF_SurahHeader_COLOR-Regular.ttf` | 1,832,408 B | **0** |
| `public/fonts/sakina/KFGQPC_Uthmanic_Script_HAFS_Regular.otf` | 246,428 B | **0** |

لا تُحمَّل تلقائيًا (لا `<link>`) لكنها تُنسخ لكل deployment وتُتاح للعامة. `docs/official-font-map.md` يذكرها كخريطة مرجعية فقط.

**أ4. `surah-header-frame.webp` = 460 KB بصيغة lossless**
```
public/images/quran/surah-header-frame.webp: RIFF, Web/P image, lossless, with alpha
```
محمّلة بـ`loading="eager" fetchPriority="high"` (`QuranPreview.tsx:191`) وبأبعاد 2400×775 بينما تُعرض داخل `.surah-frame` بارتفاع `1.42em` (≈26px على font-size 18px). **أكبر بند يمكن تحسينه في LCP.**

**أ5. 36 كلمة قرآنية قابلة للنقر لكن لا يمكن الوصول لها بلوحة المفاتيح**
- `QuranPreview.tsx:198` يُنتج `<span class="qcf-preview-word qcf-word" aria-hidden="true">`
- `globals.css:2387` يمنحها `cursor:pointer`، و`QuranPreview.tsx:145-154` يربط عليها `click`
- لا `role="button"`، لا `tabIndex`، لا معالج keydown → **مستحيل على مستخدم الكيبورد أو قارئ الشاشة تشغيل آية معيّنة**
- كما أنها `aria-hidden="true"` وهي تفاعلية — تناقض دلالي.

**أ6. صفر أنماط تركيز (focus) في الملف كله**
`grep -c "focus-visible\|:focus" src/app/globals.css` → **0**. كل الأزرار والروابط تعتمد على outline المتصفح الافتراضي، وهو غير منسجم مع الهوية.

**أ7. لا توجد قائمة موبايل**
`globals.css:231` → `.nav-links{display:none}` عند ≤900px. على الموبايل يظهر فقط الشعار + زر التحميل، وتختفي روابط المميزات/القرآن/الأذكار/الصلاة تمامًا. لا hamburger، ولا Sheet، ولا Drawer.

**أ8. روابط ميتة**
| الموضع | الرابط |
|---|---|
| `Header.tsx:34` (الشعار) | `href="#"` |
| `FinalCta.tsx:12` (App Store) | `href="#"` |
| `FinalCta.tsx:21` (Google Play) | `href="#"` |
| `Footer.tsx:26` (سياسة الخصوصية) | `#download` — لا توجد صفحة سياسة |

`README.md:65` يعترف بذلك: «أي روابط تحميل أو تكاملات خارجية غير مكتملة…».

**أ9. كل محتوى الصلاة ثابت (mock)**
`PrayerSection.tsx:1-6` — array ثابت بأربعة مواقيت، والتاريخ مكتوب يدويًا «الأحد، 21 يونيو»، والموقع «القاهرة»، والمتبقي «2:34». `stroke-dashoffset="92"` ثابت أيضًا. نفس الشيء في `Hero.tsx:98-126` (9:41، العصر، 3 محرم 1448).

### 🟠 ب — نظافة وصيانة

**ب1. `src/components/ui/` — 48 ملف مكوّن shadcn غير مستخدم إطلاقًا**
التحقق: `grep -rn "components/ui" src` خارج مجلد `ui/` نفسه → **لا شيء** عدا `src/hooks/use-toast.ts` (وهو نفسه غير مستورد من أي مكان). `examples/websocket/frontend.tsx` يستورد منها لكنه **مستثنى** من `tsconfig.json` (`"exclude": ["node_modules","examples"]`).

**ب2. تبعيات غير مستخدمة في `src`**
`framer-motion`, `next-auth`, `@tanstack/react-query`, `next-intl`, `next-themes`, `zustand`, `sonner`, `recharts`, `zod`, `lucide-react`, `embla-carousel-react`, `vaul`, `cmdk`, `date-fns`, `input-otp`, `@mdxeditor/editor`, `@dnd-kit/*`, `react-hook-form`, `@hookform/resolvers`, `react-markdown`, `react-resizable-panels`, `react-syntax-highlighter`, `@reactuses/core`, `@tanstack/react-table`, `uuid`, `sharp`, `next-auth`.
التحقق: `grep -rn "framer-motion\|next-auth\|…" src | grep -v components/ui` → لا شيء.

**ب3. `prisma` في `postinstall` رغم أن صفحة الهبوط لا تستخدم قاعدة بيانات**
`src/lib/db.ts` موجود لكن لا يستورده أي ملف. `postinstall: prisma generate` نجح محليًا (Prisma Client v6.19.3) لكنه يضيف زمن تثبيت وهشاشة بلا فائدة.

**ب4. `tests/*.sh` معطّلة**
الثلاثة scripts تبدأ بـ`SCRIPT_DIR="$(cd "$(dirname "$0")/../.zscripts" && pwd)"` — ومجلد `.zscripts/` **غير موجود** (مُدرج في `.gitignore`). لا يمكن تشغيلها.

**ب5. ~400 سطر CSS ميت**
| البلوك | الأسطر | السبب |
|---|---|---|
| `.mushaf*` القديم | 1339–1581 | المكوّن يستخدم `.qcf-*` و`.mushaf-preview-shell` |
| `.qcf-preview-page/-line/-glyph` | 2294–2319 | غير مستخدمة في JSX |
| `.mushaf-ornament-word` (SuraNames) | 2285–2293 | البرواز الآن صورة webp |
| `.prayer-pill` + `.p1..p4` + `floatPill` | 1813–1841 | استُبدلت بـdashboard (و`docs/prayer-section-review.md` يؤكد: «عدد عناصر `.prayer-pill` القديمة = 0») |
| `.gem-rim-glow`, `.cut-crystal-capsule*` | 2518–2524 | لا تُستخدم في JSX |
| `@font-face QCF_P001` مكرر | 2260 و2442 | تعريفان لنفس الخط |

**ب6. `tailwind.config.ts` غير فعّال**
- `globals.css:1` يحتوي `@import "tailwindcss"` فقط — **لا يوجد `@config`** → في Tailwind v4 لا يُحمَّل `tailwind.config.ts` إطلاقًا.
- إذًا `theme.extend` (ألوان shadcn) و`tailwindcss-animate` **غير مطبّقين**.
- كما أن `content` يشير إلى `./pages/**` و`./components/**` و`./app/**` بينما الكود في `./src/**` — مسارات خاطئة حتى لو حُمِّل.
- صفحة الهبوط لا تستخدم أي Tailwind utility (كل الـ`className` أسماء CSS مخصّصة).

**ب7. `eslint.config.mjs` يعطّل ~25 قاعدة**
منها `@typescript-eslint/no-explicit-any`, `react-hooks/exhaustive-deps`, `no-unused-vars`, `prefer-const`. لا توجد اختبارات وحدة ولا `typecheck` مستقل في CI (يعتمد على `next build`).

### 🟡 ج — ملاحظات دقيقة

- **ج1.** `siteUrl = https://sakina-noor.vercel.app` في `layout.tsx:4`, `robots.ts:3`, `sitemap.ts:3`. **التحقق:** هذا النطاق يُرجع 200، بينما `homepage` المسجّل في GitHub (`sakina-islamic-companion.vercel.app`) يُرجع **404**. أي أن الكود صحيح وحقل homepage في GitHub هو القديم.
- **ج2.** `og-image.svg` يستخدم `font-family="Thmanyah Sans"` — زواحف التواصل الاجتماعي لا تحمّل الخطوط المحلية، فالنص العربي سيسقط لخط نظام. يُفضَّل PNG أو SVG بحروف مرسومة (outlined).
- **ج3.** التلاوة من `everyayah.com` (طرف ثالث) — `QuranPreview.tsx:5-13`، 7 ملفات mp3. `preload='none'` جيد، لكن لا يوجد fallback إذا فشل CDN.
- **ج4.** `content-visibility:auto` + `contain-intrinsic-size:800px` (`globals.css:88`) مع `window.scrollTo(top - 80)` في `ScrollEffects.tsx:41` — قد يسبّب قفزات في موضع الوصول للأنكور قبل اكتمال القياس.
- **ج5.** DOM manipulation مباشر (`document.getElementById`, `querySelector`) بدل refs في `Header`, `Hero`, `QuranPreview`, `ScrollEffects`. يعمل، لكنه ليس نمط React ويُصعّب إعادة الاستخدام.
- **ج6.** `next.config.ts` يضيف Cache-Control مخصص لـ`/_next/static/*` → تحذير في كل build (ظهر في build.log).
- **ج7.** `package.json` `start` يستخدم `bun` — غير متوفر في كل البيئات (غير موجود في بيئة العمل هنا).
- **ج8.** Hero يعلن «15+ ميزة روحية» بينما Bento يعرض 9 خلايا فقط، وStats يذكر «67+ تصنيف أذكار» بينما AzkarSection يعرض 8 بطاقات. أرقام تسويقية مقابل محتوى معروض.
- **ج9.** `download/README.md` سطر واحد («Here are all the generated files.») — بقايا أداة توليد.
- **ج10.** `mini-services/.gitkeep` و`Caddyfile` و`examples/` — بقايا قالب المنصة، لا علاقة لها بصفحة الهبوط.

---

## 7) نتائج التشغيل الفعلية

| الأمر | النتيجة |
|---|---|
| `npm install` | ✅ 839 حزمة في ~104 ثانية، `prisma generate` نجح (v6.19.3) |
| `npm run build` (`next build`) | ✅ **exit 0** — Next.js 16.3.4 (Turbopack)، تجميع 12.9s، TypeScript 8.1s، 6 صفحات ثابتة |
| المسارات المولّدة | `○ /`، `○ /_not-found`، `ƒ /api`، `○ /robots.txt`، `○ /sitemap.xml` |
| `npm run lint` | ⚠️ **exit 1** — خطأان (انظر تحت) |
| `next dev` | ✅ Ready in 394ms، `GET /` → 200 (77,910 bytes)، `<title>سكينة \| رفيقك الروحي اليومي</title>` |
| الأصول (9 خطوط + صورة + svg + robots + sitemap + api) | ✅ كلها **200** |
| `bun` | ❌ غير متوفر في بيئة العمل هنا — استخدمت `npm` |

### عن خطأَي الـlint

```
src/components/ui/carousel.tsx:98:5   react-hooks/set-state-in-effect
src/hooks/use-mobile.ts:14:5          react-hooks/set-state-in-effect
✖ 2 problems (2 errors, 0 warnings)
```

**هذا فرق بيئة، لا عيب في المستودع.** التحقق:
| | عندي | في `bun.lock` (ما يستخدمه CI) |
|---|---|---|
| `eslint-config-next` | 16.3.4 | **16.1.3** |
| `eslint-plugin-react-hooks` | — | **7.0.1** |
| `eslint` | 9.39.5 | **9.39.2** |

لا يوجد `package-lock.json` في المستودع، فـ`npm install` حلّ إصدارات أحدث من المثبّتة في `bun.lock`. وبالفعل **CI على GitHub أخضر**: آخر 5 runs كلها `completed / success`، ومنها run على نفس الـcommit `692c67c` ([run 33619965619](https://github.com/msayed-io/sakina-noor/actions/runs/33619965619)).

كلا الملفين المعنيّين **غير مستخدمين في صفحة الهبوط** أصلًا (انظر ب1). لو أردت لانت نظيفًا بـnpm أيضًا، فالحل: تعطيل `react-hooks/set-state-in-effect` في `eslint.config.mjs` (متسق مع بقية القواعد المعطّلة) أو إصلاح الملفين.

---

## 8) خريطة التعديل السريع

| لو عايز تغيّر… | المكان |
|---|---|
| لون الهوية/الذهبي | `globals.css:10` + `:2474` (الطبقتان) |
| الخطوط | `globals.css:2544-2589` (التعريف) + `:2596-2600` (التطبيق بـ`!important`) |
| عنوان/نص قسم | ملف المكوّن في `src/components/landing/` |
| شبكة الأذكار | `globals.css:2631-2642` (الكلمة الأخيرة، 12 عمود) |
| لوحة الصلاة | `globals.css:2605-2629` |
| بطاقات المميزات | `Features.tsx` + `globals.css:2526-2542` |
| صفحة المصحف | `QuranPreview.tsx:15-78` (البيانات) + `globals.css:2403-2468` |
| Metadata/SEO | `src/app/layout.tsx` |
| ترتيب الأقسام | `src/app/page.tsx` |
| حركات الظهور | `ScrollEffects.tsx` + `globals.css:2228-2255` |

---

## 9) ما لم أتحقق منه

- **لم ألتقط لقطات شاشة بعد** — لا يوجد headless browser مثبّت في بيئة العمل، فالتقييم البصري الحالي مبني على قراءة CSS/JSX والقيم الحسابية، لا على صورة.
- **لم أشغّل `bun`** — غير متوفر؛ كل الأوامر أعلاه بـ`npm`/`npx`.
- **لم أختبر الصوت فعليًا** (`everyayah.com` يحتاج متصفحًا حقيقيًا وسياسة autoplay).
- **لم أفحص Lighthouse/Performance رقميًا** — التقديرات في §6 مبنية على أحجام الأصول وطريقة تحميلها.

---

## 10) ملحق — سجل التعديلات المنفَّذة (نفس اليوم)

بقرار من المالك: **تنظيف شامل + حرية تصميم كاملة**. نُفّذ الآتي وتحقّقنا منه بالبناء واللقطات:

### الحذف
- `src/components/ui/` (48 ملفًا)، `src/hooks/`، `src/lib/utils.ts`، `src/lib/db.ts`، `prisma/`، `examples/`، `tests/`، `mini-services/`، `download/`، `components.json`، `tailwind.config.ts` (غير مُحمّل في Tailwind v4).
- الخطوط غير المستخدمة (~2.19 MB): `KFGQPC…otf`، `QCF_SurahHeader_COLOR-Regular.ttf`، `sura_names.woff2`، ونسخة `thmanyahserifdisplay-Black.woff2` المكررة خارج `sakina/`.
- ~270 سطر CSS ميت (بلوك `.mushaf*` القديم، `.qcf-preview-*`، `.prayer-pill`، primitives غير مستخدمة، `@keyframes` يتيمة) عبر `tools/prune-dead-css.py`.

### الإصلاح
- حذف `@font-face` المكرر لـ`QCF_P001` والبلوك المتعارض `Thmanyah Serif Display@400`، وإضافة `font-weight:900` صريحًا لعنصري لوحة الصلاة اللذين كانا يرثان 400.
- استرجاع سطح بطاقة مشغّل التلاوة `.mushaf-player` بلمسة Sakina Pro (كان حُذف خطأ أثناء التنظيف ثم أُعيد موحدًا على 28px مع بقية البطاقات).
- `surah-header-frame.webp`: 460 KB lossless → **70 KB** lossy q95 بمقاس 1200×388 (مقارنة بصرية مطابقة)، وتحديث `width/height` في JSX.

### الإضافة
- قائمة موبايل (همبرجر) بلوحة كريستالية + إغلاق بـEscape وبضغط الروابط.
- أنماط `:focus-visible` بهوية العسل لكل العناصر التفاعلية.
- كلمات المصحف صارت أزرار لوحة مفاتيح (`role=button`، `tabIndex=0`، Enter/Space) مع `aria-label`.
- `scroll-padding-top` لتعويض الهيدر العائم عند الوصول للأنكرات.

### الاعتماديات والإعدادات
- `package.json`: 66+9 → **4+8** (next/react/react-dom/sharp + أدوات البناء). الإصدارات المثبتة في `bun.lock` محدثة ومختبرة (`install --frozen-lockfile` سيتحقق في CI).
- `eslint.config.mjs` صارم الآن (القوائم المعطّلة حُذفت مع الكود الذي كانت تخدمه) — والـlint أخضر.
- `start` صار بـ`node` بدل `bun` ليعمل في أي بيئة.
- README محدث (بنية المشروع والملاحظات).

### التحقق النهائي
| الفحص | النتيجة |
|---|---|
| `bun run lint` | ✅ exit 0 |
| `bun run build` | ✅ exit 0 (Next 16.3.4) |
| لقطات Chrome headless (ديسكتوب 1440 + موبايل 390 + القائمة المفتوحة) | ✅ كل الأقسام تظهر، RTL سليم، المصحف والقائمة والمشغل يعملون |
| كونسول المتصفح (نسخة الإنتاج) | ✅ صفر أخطاء/تحذيرات (لا hydration mismatch) |

ملاحظة أداة الالتقاط: المساحات الفارغة التي قد تظهر في لقطات الصفحة الكاملة سببها `content-visibility:auto` في وضع headless فقط؛ أُعطّل مؤقتًا داخل أداة اللقطات ولا يؤثر على المستخدمين الحقيقيين.
